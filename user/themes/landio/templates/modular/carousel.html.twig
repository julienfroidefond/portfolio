<section class="section-carousel text-center bg-inverse">
  <div class="container">
    {{ page.content }}
    <div id="carousel-content" class="carousel slide" data-ride="carousel" data-interval="0">
      <div class="carousel-inner" role="listbox">
        {% for testimonial in page.header.items %}
        <div class="carousel-item {% if loop.first %}active{% endif %}">
          <div>
            {% if testimonial.avatar %}
            <img src="{{ page.media.images[testimonial.avatar].url }}" height="80" width="80" alt="Avatar" class="img-circle">
            {% endif %}
            {% if testimonial.quote %}
            <p class="h3">{{ testimonial.quote }}</p>
            {% endif %}
            {% if testimonial.author %}
            <footer>{{ testimonial.author }}</footer>
            {% endif %}
          </div>
        </div>
        {% endfor %}
      </div>
      {#<ol class="carousel-indicators">
        {% for testimonial in page.header.items %}
        {% if testimonial.avatar %}
        <li {% if loop.first %}class="active"{% endif %}><img src="{{ page.media.images[testimonial.avatar].url }}" alt="Navigation avatar" data-target="#carousel-content" data-slide-to="{{ loop.index0 }}" class="img-responsive img-circle"></li>
        {% endif %}
        {% endfor %}
      </ol>#}
    </div>
  </div>
</section>
